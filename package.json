{
  "name": "contentful-context-toolkit",
  "version": "0.1.0",
  "type": "module",
  "description": "Lightweight toolkit for Contentful GraphQL introspection, mapping, and content snapshotting.",
  "scripts": {
    "introspect": "node src/schema/introspect.js",
    "generate-map": "node src/schema/contentMap.js",
    "fetch-content": "node src/queries/fetchAllContent.js",
    "context": "npm run introspect && npm run generate-map && npm run fetch-content",
    "docs": "node src/docs/generateDocs.js",
    "context-docs": "npm run context && npm run docs"
  },
  "dependencies": {
    "graphql-request": "^7.0.0",
    "dotenv": "^16.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0"
  }
}
